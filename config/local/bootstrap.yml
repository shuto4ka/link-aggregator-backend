spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: local-link-aggregator-backend
  cloud:
    config:
    uri: http://localhost:8888

management.endpoints.web.exposure.include: ["refresh", "health"]
