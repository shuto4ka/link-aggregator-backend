spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: dev-link-aggregator-backend
  cloud:
    config:
    uri: http://config-server:8888

management.endpoints.web.exposure.include: ["refresh", "health"]
